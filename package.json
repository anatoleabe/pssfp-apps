{
  "name": "pssfp",
  "version": "0.0.0",
  "private": true,
  "description": "Mono-repo PSSFP — site institutionnel, bibliothèque virtuelle, candidature en ligne, API Laravel.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "lint": "pnpm -r --parallel run lint",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r --parallel run typecheck",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
