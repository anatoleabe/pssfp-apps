# Template de /etc/default/minio — copier sur le VPS en mode 600, root only.
# AUCUNE valeur réelle ici (repo public de fait).
MINIO_ROOT_USER=__a_definir__
MINIO_ROOT_PASSWORD=__a_definir__
MINIO_VOLUMES=/srv/minio/data
